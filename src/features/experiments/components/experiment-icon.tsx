import { IconFlask } from "@tabler/icons-react";

/**
 * Icon đại diện cho experiment. Khai báo một chỗ duy nhất để switcher và
 * breadcrumb dùng chung — đổi icon chỉ cần sửa ở đây.
 */
export const ExperimentIcon = IconFlask;
